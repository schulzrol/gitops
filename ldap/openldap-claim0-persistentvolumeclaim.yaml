apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: openldap-claim0
  name: openldap-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
